<div id="message-<%= message.content %>">
  <small>
    <strong><%= message.user.first_name %></strong>
    <%# <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %>
  </small>
  <p><%= message.content %></p>
</div>
